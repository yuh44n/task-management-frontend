[build]
builder = "nixpacks"

[build.nixpacksConfig]
packages = ["php", "php-fpm", "nginx", "composer"]

[build.nixpacksConfig.phases.build]
cmds = ["composer install --no-dev --optimize-autoloader"]

[deploy]
startCommand = "php-fpm -D && nginx -g 'daemon off;'"
